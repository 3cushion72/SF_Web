<?xml version="1.0" encoding= "UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd" >

<configuration>

	<!-- 
		1) properties
		2) settings
		3) typeAliases
		4) typeHandlers
		5) objectFactory
		6) plugins
		7) environments
			= environment
				== transactionManager
				== dataSource
		8) mappers
	 -->

	<properties>
		<!-- 
			* dataSource 에 대한 설정은 외부에 있으므로 내부적으로 사용하는 property만 설정할 것.  
		-->
	</properties>
	<settings>
		<!-- 
			* 런타임시 Mybatis의 행위를 조정하는 값 설정.
		-->
		<setting name="cacheEnabled" value="true"/>
		<setting name="lazyLoadingEnabled" value="true"/>
		<setting name="multipleResultSetsEnabled" value="true"/>
		<setting name="useColumnLabel" value="true"/>
		<setting name="useGeneratedKeys" value="false"/>
	</settings>
	<typeAliases>
		<!-- 
			* 자바타입에 대한 짧은 별칭 설정
			* VO의 별칭을 사용해도 되지만 건건이 추가는 위험하므로 해당 sql.xml에서 풀경로를 사용한다.  
		-->
	</typeAliases>
	<typeHandlers>
		<!-- 
			* MyBatis가 PreparedStatement 에 파라미터를 셋팅하고 ResultSet 에서 값을 가져올때마다,
			* TypeHandler 는 적절한 자바 타입의 값을 가져오기 위해 사용된다. 
			* 디폴트 사용
		-->
	</typeHandlers>
    <mappers>
		<!-- 
			* 매핍되는 sql.xml을 지정
			* 외부 파일의 mappers 프로퍼티 설정으로 세팅이 되어 있음.  
		-->
    </mappers >
</configuration>
